/**
 * Describes the default state for the menu button, meaning no specific value has been set.
 */
export interface MenuButtonDefault {
  /**
   * Type of the button, must be "default".
   */
  type: 'default'
}
