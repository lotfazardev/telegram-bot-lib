/**
 * Represents a state where the revenue withdrawal is in progress.
 *
 */
export interface RevenueWithdrawalStatePending {
  /**
   * Type of the state, always “pending”.
   */
  type: 'pending'
}
